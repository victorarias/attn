# Portable Workflow Engine — Execution Tracker (autonomous goal)

**Branch:** `feat/workflow-engine` · **Worktree:** `/Users/victor/projects/victor/attn--feat-workflow-engine`
**Design:** [2026-06-14-portable-workflow-engine.md](2026-06-14-portable-workflow-engine.md) (v2)
**Driver:** self-paced `/loop`. Each step = one ultracode workflow (spec→build→verify→fix). Advance on workflow-completion notifications; long fallback heartbeat in case a workflow hangs.

## Rails (non-negotiable)

1. All work on `feat/workflow-engine` in the worktree. **Never** touch `main` or prod; **never** `make install` / packaged-app / app builds autonomously (needs keychain/prod).
2. Commit a step **only** when independently verified green: `cd <worktree> && go build ./...` + that step's `go test`. Report honestly; do not claim green if not.
3. One step → one ultracode workflow, scoped to that step's files only (sequential builder, parallel read-only verify, conditional fixer — no parallel editors).
4. **PAUSE at HUMAN-GATE steps** — implement + unit/integration-test on the branch, then STOP the loop and surface to Victor with exactly what's needed. Do not auto-proceed past a gate.
5. Migration numbering: verify live `MAX(version)` in real `~/.attn` + `~/.attn-dev` DBs first (burned-version caveat). ProtocolVersion bumps are gates (need coordinated daemon/app + real-daemon check).
6. Keep this tracker current each step (status + commit sha). Trust `go build` over gopls (multi-worktree false diagnostics).

## Steps

| # | Step | Scope | Verify | Gate | Status |
|---|------|-------|--------|------|--------|
| E1 | Engine spine (fake `agent()`) | `internal/workflow`: goja realm + determinism bans + watchdog + **structural ordinals** + journal iface + kill/resume + caps | `go test ./internal/workflow/... -race` | auto | **✅ done** |
| E2 | Real headless `agent()` | generalize `RunHeadlessTask` (generic result+text capture, per-call `ResultPath`); `return_result` MCP tool w/ **in-turn** schema validation + retry + detect-missing-call + error→null; no-schema text path | `go test` + a real `codex exec` round-trip | auto¹ | **✅ done** |
| E3 | Writable exec + MCP attach | writable working-tree sandbox (re-enable edit/shell) that mutates tree **and** returns schema-valid result; attach session MCP servers to subagent | `go test` + writable round-trip | **GATE** (security review of re-enabling write/shell in headless) — **APPROVED by Victor 2026-06-14** | **✅ done** |
| S-store | Durable journal | `workflow_runs` (+`session_id`/`workspace_id`) + `workflow_agent_calls` migration + store methods; swap E1 in-memory `Journal` for the durable impl | `go test ./internal/store/...` | auto (migration-number check) | **✅ done** |
| S-proto | Protocol | TypeSpec `WorkflowRun*` models + `make generate-types` + `constants.go` cmds/events + **ProtocolVersion bump** | `go build` + `tsc` | **GATE** (version bump → coordinated daemon/app) — **APPROVED by Victor 2026-06-14** | **✅ done** |
| S-daemon | Daemon IPC + runner | `workflow_run_upsert/call_upsert/get/list/cancel`; coalesced broadcast; async runner; cancel relay; completion `Source` adapter | `go test ./internal/daemon/... -run Workflow` | auto² | **✅ done** |
| S-cli | Agent-facing contract | `attn workflow run/result/show/list` + `--args`/`--args-file` + runId→`ATTN_SESSION_ID` attach + authoring reference (`internal/agent/attn_skill/references/workflow.md`) | `go test` + CLI smoke | auto | **✅ done** |
| S-fe | Read-only UI | `WorkflowRunView` (clone `SessionReviewLoopBar`, strip controls) + `useDaemonSocket` wiring + store slice + **App.tsx dock-panel mount** | `pnpm test` (unit) | **GATE** (packaged-app visual verify needs `make`) | **impl+mount ✅ `cfa5a20c`/`e7b75891`** · packaged-app visual = GATE |
| E4 | E2E skeleton harness | dev-profile loop: agent triggers → engine → journal → coalesced broadcast → UI → `attn workflow result` → cancel → resume | real-app scenario on dev profile | **GATE** (needs `make dev` / real daemon) | **core proven live** · UI-mount + WS/packaged-app + live-writable remain |
| E5 | Fan-out + caps | `parallel`/`pipeline` never-reject + null-slot + stage sig; concurrency `min(16,cores-2)`; ordinals stable under real concurrency; 4096 guard | `go test ./internal/workflow/... -race` | auto | **✅ done** |
| E6 | Worktree isolation | `isolation:'worktree'` over attn worktree infra; §6 side-effect/replay contract | `go test` + worktree round-trip | auto | **✅ done** |

¹ E2 needs working `codex` headless auth (OPENAI creds) for the live round-trip; if absent, implement + unit-test and flag the live check.
² real-daemon E2E for S-daemon is folded into E4 (a gate); S-daemon itself is unit/integration-tested.

## Order & dependencies

Spine first: **E1 → E2 → E3(gate)**. Then persistence/transport: **S-store → S-proto(gate) → S-daemon → S-cli**. Then surface + proof: **S-fe(gate) → E4(gate)**. Then completeness: **E5 → E6**. The loop runs each `auto` step to green-and-commit, and stops at the first `GATE` it reaches with a report. Auto steps that have no gate dependency ahead of a gate may be reordered earlier to maximize unattended progress (e.g. S-store, S-cli, E5 are gate-free Go work).

## Log

- **E1 — engine spine: DONE.** 32 tests green under `-race` (3201 LOC). goja realm + deny-by-default + determinism bans (incl. the `Date()`-as-function bypass), `vm.Interrupt` watchdog, **structural ordinals** (incl. the post-await parallel/pipeline temporal-leak fix the review caught), in-memory `Journal` + resume (R1–R5 + kill@k), lifetime/concurrency/item caps. R-spec (`2026-06-14-resume-invalidation-spec.md`) + this tracker committed alongside. The adversarial review found 3 real must-fix defects (post-await temporal ordinals breaking R1, `Date()` bypass); the fix phase resolved them with regression tests that fail pre-fix / pass post-fix.
- **E2 — real headless `agent()`: DONE.** Generalized `RunHeadlessTask` (`Schema`/`ToolName`/`ResultPath` + `Result`/`Text` capture); new `internal/workflowresult` schema-validating `return_result` sink (jsonschema v6, in-turn `isError` self-correction, last-write-wins) + `attn _workflow-result-mcp` subcommand; `driverAgent` wires `agent()` to real spawns with detect-missing + outer re-spawn retry + error→null + schema/text paths. **Live `codex exec` round-trip returned a schema-valid `{"answer":"PONG"}`.** Janitor untouched; all affected packages green under `-race`; review found 0 must-fix. `driverAgent` is built+tested but not yet wired into a production caller (that's E4's `attn workflow run`).
- **S-store — durable journal: DONE.** Migration 50 (`workflow_runs` + `workflow_agent_calls`, declarative `CREATE … IF NOT EXISTS`, idempotent on reopen); store CRUD in `workflow_runs.go` (store-local row structs, modeled on `review_loop_runs.go`; manual child-delete since FK CASCADE is documentary store-wide — `PRAGMA foreign_keys` is never enabled); `DurableJournal` adapter (`workflow` imports `store`, no cycle) with a write-through mem mirror seeded from SQLite so a fresh adapter resumes from the DB alone. **Journal-parity test runs E1's R1–R5 + kill@k over both `[mem, durable]`**, durable arm rebuilt from SQLite. Green under `-race`; review 0 must-fix. (Adapter writes the 6 `JournalEntry` fields; the richer columns — label/phase/resolved_model/result_path/timestamps — are populated by the S-daemon write path.)
- **E6 — worktree isolation: DONE.** `AgentStub.Run` evolved from `(ordinal, prompt, schema)` to a single `AgentCall` spec struct (`{Ordinal, Prompt, Schema, Isolation, Model, AgentType}`) for native `agent(prompt, opts)` parity; every implementer migrated (`DefaultStub`/`StubFunc`/`ScriptedStub`/`driverAgent` + all test stubs across `internal/workflow/*_test.go` and `cmd/attn/workflow_test.go`). `host.go` extracts `isolation`/`model`/`agentType` off the opts object alongside the schema (`extractAgentString` + `validateIsolation` normalize unknown isolation → `""`) and threads them into the `AgentCall` — **the journal cache predicate is unchanged** (still ordinal AND prompt_hash AND schema_hash; isolation/model are NOT cache identity). `driverAgent.Run` dispatches `isolation:'worktree'` to `runIsolated`: resolve repo root via `git.ResolveMainRepoPath`, create a fresh worktree on `attn-wf/<short-ordinal-hash>` (`GenerateWorktreePath`), run the subagent with that worktree as CWD (workspace-write; scratch stays in runTmpDir), then **outcome-independent cleanup** keyed on the new `git.IsWorktreeClean` (`status --porcelain --untracked-files=all`): clean → `DeleteWorktree` + best-effort branch prune; dirty → KEPT + logged (even on a failed run). `CreateWorktree` failure fails closed → error → engine null (never falls back to the shared tree). `isolation:''` is byte-identical to E3 (CWD = shared working tree). New `internal/workflow/isolation_test.go`: host-threading (recording stub asserts isolation/model/schema threaded; no-opt/unknown → `""`) + **real-git hermetic round-trip** (no daemon/codex; injected fake runner): Case A mutating → result returned AND worktree KEPT; Case B no-op → worktree auto-removed; Case C isolation `""` → runs in working tree, no worktree; plus failed-run-keeps-dirty + per-call model override. Verified: `go build ./...`, `go test ./internal/workflow/... -race -count=1`, `go test ./cmd/attn/... -run Workflow -count=1`, `go test ./internal/git/...` — all green; resume/ordinal/concurrency suites pass unchanged (isolation defaults to `""`).
- **E5 — fan-out + caps: DONE (test-only).** The concurrency cap (`Config.ConcurrencyCap`, `defaultConcurrency()` = `min(16,cores-2)`, semaphore-gated) and the never-reject/null-slot/4096 guards all shipped in E1; E5 is a closeout that *proves* them under genuine concurrency rather than changing production code — `engine.go` is byte-identical to HEAD. New `internal/workflow/concurrency_test.go` (5 tests): `parallel`/`pipeline` reach the cap and never exceed it (atomic high-water-mark via a `blockingStub` that parks inside the semaphore critical section), ordinals stay stable under a `runtime.Gosched`-jittered `raceStub`, and `parallel`/`pipeline` keep null slots/items under concurrency. Builder tamper-verified the teeth (made the semaphore unbounded → cap tests failed → restored). Green under `-race` + `-count=10` on the concurrency/ordinal/cap tests (no flakiness); adversarial review all-true (`concurrency_bound_proven`, `ordinal_stable_under_real_concurrency`, `no_duplicate_or_weak_tests`, `no_regression`), `must_fix: []`.
- **GATES APPROVED (2026-06-14).** Victor approved both E3 (writable-exec security review) and S-proto (ProtocolVersion bump, on-branch). Loop resumed to drive the whole plan with ultracode; only the genuinely `make`-dependent verifications (S-fe packaged-app, E4 real-daemon) remain as a final batched handoff.
- **S-cli — agent-facing contract: DONE.** `attn workflow run|result|show|list` (cmd/attn/workflow.go). The engine runs **in the CLI process** (Model A) and reports to the daemon over the strictly-request/response unix socket. `internal/client` gains `sendWorkflow` + 5 methods decoding the daemon's `WorkflowActionResultMessage` (reply-shape match verified both sides — no latent E4 mismatch). `cmd/attn/workflow_ipcjournal.go` = the IPC journal (write-through MemJournal mirror + `workflow_call_upsert` proxy, seeded from the daemon for `--resume`, `lastErr`-tolerant like DurableJournal). **Cancellation = cooperative polling** (a watcher polls `WorkflowRunGet` every ~1s and cancels the engine root context when `Status==canceled`; propagates to in-flight subagent ctxs) — the request/response client can't read a relayed frame, so S-daemon's relay registry stays a best-effort seam. `run`: foreground `--wait` (blocks, prints the result shape) or background detach via self re-exec with `Setsid` reusing the parent runId. Terminal upsert read-modify-writes the run (store ON CONFLICT replaces all columns) so script_path/created_at/harness/session aren't clobbered; interrupted→canceled only on the engine's cancel tag, watchdog-timeout→failed. `result` emits the frozen `{status,result,error?,phase,calls_total,calls_done}` (exit 0 completed / non-zero failed); os.Exit logic factored into pure testable helpers. `references/workflow.md` = the frozen authoring contract (auto-embedded; host fns + meta-first-literal rule + determinism bans **with substitutes** + all four verbs). Verified: `go build`, `go test ./cmd/attn/... -run Workflow -race` (`-count=3` no flake), client+full-cmd regression, `workflow --help` smoke; 4 verifiers all-pass, 0 must-fix. Real-daemon round-trip is E4.
- **S-daemon — daemon IPC + broadcast + cancel + completion: DONE.** Mirrors the review-loop split (shared core in `workflow_run.go` + thin per-transport wrappers): 5 commands wired on BOTH the socket dispatch (engine/CLI, `net.Conn`) and WS dispatch (read-only UI, `*wsClient`). `workflow_broadcast.go` = coalescing broadcaster (dirty-set + directly-callable `flushWorkflowBroadcasts` → one full-hydrated-run `workflow_run_updated` per dirty run; ~75ms prod ticker; self-healing against the 256-buffer slow-client drop). `workflow_engine_registry.go` = cancel-relay registry (`workflowEngineSink` interface; conn/ws adapters; the socket upsert handler registers the engine conn so `workflow_run_cancel` relays a control frame to it). `attention.go` = the daemon's production `attention.Aggregate` caller (reads sessions/PRs/repos/authors + hydrated workflow runs from the store; **makes the workflow→attention completion path live**, invoked from the broadcast flush); `WorkflowRunAdapter` surfaces completed/failed runs only — and `internal/attention` still imports **only** `protocol` (no `store`, no cycle). Verified: `go build ./...`, `go test ./internal/daemon/... -run Workflow -race` (8 tests, `-count=3` no flake), full daemon regression (no `-race` per the known [[gitstatus-scheduler-race]]), attention + store-Workflow all ok; 4 verifiers all-pass, 0 must-fix. Note: `internal/attention` had **no** prior production caller — S-daemon adds the first one. Live engine round-trip (real socket conn + cancel relay) is E4.
- **S-proto — protocol: DONE.** `main.tsp` gained 2 enums (`WorkflowRunStatus {running,completed,failed,canceled}`, `WorkflowAgentCallStatus {running,ok,skipped,errored}` — kept intentionally non-identical so quicktype doesn't merge them), 2 domain models (`WorkflowRun`, `WorkflowAgentCall` mirroring the S-store rows), 5 command models + 2 event models (events registered in the `DaemonEvent` union). `make generate-types` rewrote `generated.go`/`generated.ts` and is **deterministic** (md5 stable across reruns → CI `check-types` safe). `constants.go`: 5 `Cmd*` + 2 `Event*` consts + 5 `ParseMessage` cases + **`ProtocolVersion "105"→"106"`**; frontend `PROTOCOL_VERSION '105'→'106'` in lockstep (`useDaemonSocket.ts:171`). Purely additive (no existing model/version-logic touched). Verified: `go build ./...`+`go test ./internal/protocol/...` ok, both enums distinct in `generated.ts`, `tsc --noEmit` exit 0; 4 parallel verifiers all-pass, 0 must-fix. **Note for S-fe:** quicktype emitted a cosmetic duplicate `Call` interface structurally identical to `WorkflowAgentCall` (`run.agent_calls` types as `Call[]`); harmless via structural typing, alias if it bites. The version bump means once this branch is `make install`-ed, old daemons force-reconnect by design (Victor's coordinated rebuild, not autonomous).
- **S-fe MOUNT — WorkflowRunView wired into App.tsx (`e7b75891`).** A Sidebar "Workflow Runs" tool button (new `WorkflowIcon`) toggles a new `workflowRun` dock panel rendering the read-only `WorkflowRunView`, mirroring the review-loop dock panel at **every** site (DockPanelId union, openPanels init, `workflowRunPanelOpen` derivation, sidebar-tools entry + its useMemo deps, the `<RightDock>` panels-array entry, and `listWorkflowRuns` threaded through outer-App destructure → `<AppContent>` prop pass → `AppContentProps` → AppContent destructure). New pure selector `selectLatestWorkflowRunForSession` (created_at lexicographic max; tested) picks the active session's latest run from the slice; an App `useEffect` backfills via `listWorkflowRuns(activeSessionId)` while the `workflow_run_updated` broadcast keeps it fresh. Panel passes only `run`+`onClose` (read-only). **Purely additive — no existing panel or terminal focus-ownership logic touched.** App integration-test Sidebar mocks gained `WorkflowIcon`+`listWorkflowRuns`. Verified: `tsc --noEmit` 0, **full frontend suite 971 passed (107 files)**, + an adversarial 2-verifier ultracode pass (faithfulness/exhaustiveness + data-flow/read-only) both all-pass, 0 findings. **Only the packaged-app VISUAL verify remains** (open the panel in `attn-dev`, trigger `attn workflow run`, confirm the run renders live). This also satisfies E4's "coalesced broadcast → UI shows the run under its session" leg in code; only the visual confirmation is left.
- **E4 — live dev-daemon round-trip: CORE PROVEN (2026-06-15); UI/packaged-app + live-writable remain.** `make dev` built+launched the dev sibling (`~/Applications/attn-dev.app`, port 29849, `~/.attn-dev/`, protocol **106**, git_commit @ branch HEAD) with the real signing identity (run outside the sandbox per AGENTS.md). Drove the engine **in the `attn workflow run` CLI process** against the live daemon and verified end-to-end: (1) **lifecycle** — zero-agent script → `completed`, `return` value flows back as terminal result, persisted + readable via `list`/`show`/`result`; (2) **meta-validation + error propagation** — a malformed `meta.phases` (object instead of string) recorded `failed` with the exact reason, CLI→daemon→store→readback; (3) **agent leg** — a real `codex exec` subagent → `return_result` MCP sink → schema-validated `{answer:"PONG"}`, call journaled with **structural ordinal** (`ph1/cs@…#0`), prompt/schema hashes, `status:ok`; (4) **retry + error→null** — the pre-fix model bug drove 3 attempts then `errored`→`null` with a precise error, run still `completed`; (5) **resume** — `--resume` cache-hit the journaled call in **0.012s** (no respawn), same result; (6) **cancel** — a detached multi-agent run, `attn workflow cancel <id>`, went terminal `canceled` after the engine's ~1s poll, **no orphaned engine/codex**. **Bug found+fixed live (`8f285a01`)**: empty `--model` emitted `-m ""`/`--model ""` → harness rejects as "model invalid or unavailable"; both argv builders now omit the flag when empty (harness default). **New verb (`9d92a055`)**: `attn workflow cancel` — the design's "moved-on agent halts a run" path (read-only UI has no control). **Remaining for E4 "done" (all GATE / packaged-app):** the coalesced `workflow_run_updated` broadcast reaching a WS client → **`WorkflowRunView` mounted in `App.tsx`** showing the run (S-fe mount), and the live **writable-tree mutation** (E3 path; design-gated behind `ATTN_RUN_LIVE_CODEX`). These are the real-app `scenario-workflow-*` on the dev profile.
- **S-fe — read-only UI: IMPLEMENTATION DONE (`cfa5a20c`); App-mount + packaged-app verify remain GATE.** New zustand slice `app/src/store/workflowRuns.ts` (Record keyed by `run_id`; immutable upsert/upsertBatch/remove/reset; empty-`run_id` no-op). New presentational `WorkflowRunView.tsx` (+css) — status badge by `WorkflowRunStatus`, phase/harness, `done/total` progress (done = terminal call statuses ok|skipped|errored), per-agent-call list (ordinal/label/status pill/model/error, `data-testid=workflow-call-<ordinal>`), last-error on failed, result on completed, empty-state when null. **STRICTLY read-only**: zero daemon-mutating controls; the only button is a local `Hide` gated on `onClose`; no inputs/textareas/selects; doesn't import `useDaemonSocket`. `useDaemonSocket` wiring (additive): `WorkflowRunState` alias; `workflow_run_updated` → store hydrate; `workflow_action_result` → resolve pending get/list/cancel keyed `workflow_run_get_<id>` / fixed `workflow_run_list` / `workflow_run_cancel_<id>` + slice hydrate; `getWorkflowRun`/`listWorkflowRuns` request/result methods (10s timeout, OPEN guard) exposed on the hook return. Reply shapes cross-checked against `internal/daemon/ws_workflow.go` (action strings `get`/`list`/`cancel`, fields `run`/`runs`/`run_id`/`success`/`error`); `generated.ts` untouched. vitest: slice (5), component incl. read-only guarantee (7), hook wiring (3 added to the 45-test file). **Independent verify (the gate): full frontend suite `pnpm test` = 963 passed / 5 skipped across 107 files, exit 0; `pnpm exec tsc --noEmit` exit 0.** Built via ultracode (builder → 3 parallel read-only verifiers all-pass → no fixer). **NOT mounted into `App.tsx`** — the dock-panel placement (alongside the `reviewLoop` panel) + visual/packaged-app verification is the remaining GATE, batched with E4.
- **E3 — writable exec + MCP attach: DONE.** Additive `HeadlessTaskRequest` fields (`Sandbox`, `CWD`, `ExtraMCPServers []MCPServerSpec`); pure argv builders `buildCodexHeadlessArgs`/`buildClaudeHeadlessArgs` extracted for table testing. Codex writable path = `--sandbox workspace-write` + `features.shell_tool=true`, **every other feature stays off**, `approval_policy="never"` retained (OS seatbelt is the boundary); Claude writable path adds Edit/Write/MultiEdit/Bash. Both **fail closed** on unknown sandbox values and never use `danger-full-access` / `--dangerously-bypass`. `driverAgent` is **writable-by-default** (CWD=WorkingTree→runTmpDir fallback) and threads `SessionMCPServers`→`ExtraMCPServers` for both schema/no-schema paths; scratch stays in runTmpDir so the working tree stays clean. **Janitor byte-identical** (empty git diff + `TestJanitorShapedRequestStaysReadOnly` regression). Security posture documented in code + design §7 (incl. the residual no-network-in-workspace-write follow-up). 11 new tests green under `-race -count=3` (no flakiness); 4 parallel verifiers (build/test, janitor, security, parity) all-pass, 0 must-fix. Live writable codex round-trip gated behind `ATTN_RUN_LIVE_CODEX` (manual/E4).
