# Archived Native Workspace Prototype Proposal

This directory contains the broad native workspace-client proposal and its
HTML explainer from the first GPUI workspace prototype. It established useful
foundations, including daemon-owned layouts, Ghostty rendering, automation,
and low-memory visible-pane lifetimes.

It was archived on 2026-05-24 because it mixed those foundations with a wider
product proposal before the next workflow was settled. Active work is now
scoped to copying the proven Tauri session-location picker mechanics into
workspace-first native launcher flows:

- `../../2026-05-24-native-workspace-pane-launcher.md`

Historical visual companion:

- `../../../prototypes/archive/native-workspace-prototype/index.html`

Do not treat the archived Todo/settings/diff sequence or its visual layout as
the committed next implementation order.
