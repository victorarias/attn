kind: internal
area: docs
change: >
  Garden plan and friendly-home vision refined after alignment: a
  converse-and-observe vertical (peek + directed msg) is sequenced ahead of
  the garden so retiring tickets never orphans agent conversation; tickets
  retire outright (no two-system era); the crown is authoritative from
  planting with `attn seed export` as the review bridge until native seed
  rendering; a vocabulary table with the beads mapping (distill renamed
  cutting); agents are primed at launch instead of discovering the garden;
  and consented closure mechanics — a context-pressure tripwire, then
  handoff→new→wake by default (sessions are days) or handoff→compact→wake
  as the mid-day nap — with per-role context limits and the deep version
  landing in attn's own pi-based agent.
