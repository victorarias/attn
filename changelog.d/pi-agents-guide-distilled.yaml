kind: internal
area: docs
change: >
  plugins/attn-pi/AGENTS.md now carries only attn-specific invariants and pi
  behavior that is verified but undocumented, each stamped with the pi version
  it was verified against. Everything pi's own docs answer is a pointer into
  the installed doc set at node_modules/@earendil-works/pi-coding-agent/docs/,
  and a preamble says so, so the file stops growing a section per PR.
notes: >
  Re-verified every undocumented-pi claim against the pinned 0.83.0. One was
  wrong: module-scope state survives a session transition but NOT a reload,
  which clears pi's extension module cache.
