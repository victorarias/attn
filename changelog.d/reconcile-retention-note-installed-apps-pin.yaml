kind: internal
area: apps
change: >
  The reconcile design doc now records the retention decision behind gap
  handling: installed apps pin the bus retention floor whether enabled or
  disabled, so an installed app never loses subscribed facts; a generous
  capped tripwire replaces silent trimming. Doc only — the TrimBusEvents
  floor change ships separately.
